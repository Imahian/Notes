# Passive Reconnaissance

Passive DNS scanning with DNSDumpster to identify infrastructure without directly touching the target.

## DNSDumpster

Web-based passive DNS recon tool — maps subdomains, DNS records, IPs, and MX servers from public data.

```
https://dnsdumpster.com
```

Enter target domain → get visual infrastructure map with A, MX, NS, TXT records and associated IPs.
